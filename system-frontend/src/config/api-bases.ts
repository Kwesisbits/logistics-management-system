/**
 * Compatibility shim: older HMR graphs and cached chunks may still import this path.
 * Canonical module: `src/api/service-bases.ts`.
 */
export { serviceBases, apiBases } from '../api/service-bases'
