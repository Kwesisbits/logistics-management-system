spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    driver-class-name: org.postgresql.Driver

server:
  port: 8085

logging:
  level:
    com.logistics: INFO
